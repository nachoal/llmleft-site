{
  "name": "llmleft-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Marketing and download site for LLM Left.",
  "scripts": {
    "dev": "astro dev --host --port 4321",
    "build": "astro build && node scripts/validate-site.mjs",
    "preview": "astro preview --host --port 4322",
    "check": "astro check",
    "assets": "swift scripts/render-brand-assets.swift",
    "validate": "node scripts/validate-site.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.2.8",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@tailwindcss/vite": "^4.1.14",
    "tailwindcss": "^4.1.14",
    "typescript": "^5.9.0"
  }
}

